<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background: #0a0a0a;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
      margin: 0;
      padding: 0;
      -webkit-font-smoothing: antialiased;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 40px 20px;
    }
    .logo {
      font-size: 28px;
      font-weight: bold;
      color: #ff2d7a;
      text-align: center;
      margin-bottom: 8px;
    }
    .tagline {
      color: #888;
      font-size: 14px;
      text-align: center;
      margin-bottom: 40px;
      font-style: italic;
    }
    .content {
      background: #1a1a1a;
      border-radius: 12px;
      padding: 32px;
      text-align: center;
      border: 1px solid #333;
    }
    .greeting {
      font-size: 20px;
      margin-bottom: 16px;
      color: #fff;
    }
    .message {
      color: #ccc;
      margin-bottom: 24px;
      font-size: 16px;
    }
    .code {
      font-size: 36px;
      font-weight: bold;
      letter-spacing: 8px;
      color: #00d4ff;
      background: #0a0a0a;
      padding: 16px 32px;
      border-radius: 8px;
      display: inline-block;
      font-family: 'Courier New', Courier, monospace;
      border: 1px solid #333;
    }
    .footer {
      color: #666;
      font-size: 12px;
      margin-top: 40px;
      text-align: center;
      line-height: 1.6;
    }
    .footer p {
      margin: 8px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">&#9889; Dopaminder</div>
    <div class="tagline">{{ tagline }}</div>

    <div class="content">
      <div class="greeting">{{ locale == 'pl' ? 'Hej!' : 'Hey!' }}</div>
      <p class="message">
        {{ locale == 'pl' ? 'Twój kod logowania:' : 'Your login code:' }}
      </p>
      <div class="code">{{ code }}</div>
    </div>

    <div class="footer">
      <p>{{ locale == 'pl' ? 'Kod wygasa za 10 minut' : 'Code expires in 10 minutes' }}</p>
      <p>{{ locale == 'pl' ? 'Jeśli nie prosiłeś o ten kod, zignoruj tego maila.' : "If you didn't request this code, just ignore this email." }}</p>
    </div>
  </div>
</body>
</html>
